---
description: 版本文档与主设计文档同步规则 - 编辑版本文档时需同步更新主文档
globs:
  - "投资公司发展物语 V2.0 设计文档 v*.md"
alwaysApply: false
---

# 版本文档同步规则

## 触发条件
当用户创建或修改 `投资公司发展物语 V2.0 设计文档 v*.md` 版本文档时，此规则生效。

## 必须执行的同步操作

### 1. 识别变更内容
分析版本文档中的变更：
- 新增功能/机制
- 数值调整（公式、上限、费用等）
- 初始状态变更
- 新增章节或删除过时内容

### 2. 同步到主设计文档
必须将对应变更应用到 **`投资公司发展物语 V2.0 设计文档.md`**（主文档）：

```
主文档路径: 投资公司发展物语 V2.0 设计文档.md
```

#### 同步原则
| 变更类型 | 同步方式 | 备注 |
|---------|---------|------|
| 新增功能 | 添加到主文档对应章节 | 标注「已实装」状态 |
| 数值调整 | 覆盖主文档旧数值 | 保持单一事实来源 |
| 机制变更 | 替换旧机制描述 | 如月薪公式、管理上限等 |
| 新增章节 | 在主文档新增对应章节 | 保持结构一致 |

#### 需要特别关注的同步点
- **第0节「实现约定与变更摘要」** - 汇总最新约定
- **初始状态** - 资金、员工数量、AI风格等
- **薪资公式** - 月薪计算方式
- **管理上限** - 员工各职级的AUM上限
- **新增系统** - 人才库、AI风格、月分红等

### 3. 版本历史更新
在主文档末尾添加版本历史记录，格式：

```markdown
## 版本历史

### v0.x (YYYY-MM-DD)
- 变更项1
- 变更项2
...
```

### 4. 冲突处理
如果主文档与版本文档存在冲突：
1. 以**版本文档**（最新实现）为准
2. 但保留主文档中的**长期规划/占位功能**描述
3. 在冲突处添加说明：「已实现，详见v0.x版本」

## 示例

用户更新了 `v0.2.md` 中的月薪公式：
```markdown
<!-- v0.2.md -->
月薪 = 0.5万 + 工龄×0.1万
```

则必须同步修改主文档：
```markdown
<!-- 投资公司发展物语 V2.0 设计文档.md -->
月薪 = 0.5万 + 工龄×0.1万  <!-- 已更新：v0.2 -->
```

## 禁止事项
- ❌ 不要只更新版本文档而忽略主文档
- ❌ 不要在主文档保留已过时的旧数值
- ❌ 不要删除主文档中的长期规划内容（只更新已实现部分）

## 完成后检查
- [ ] 主文档已打开并可见变更
- [ ] 第0节摘要已更新
- [ ] 相关详细章节已更新
- [ ] 版本历史已添加
- [ ] 无过时冲突内容残留
